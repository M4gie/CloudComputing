#Cloud Computing

# Start the project

- Create a .env  
  `mv .env.example .env`
- Install dependencies  
  `yarn`
- Run the project  
  `yarn start`

# Build the project

- Create a .env  
  `mv .env.example .env`
- Install dependencies  
  `yarn`
- Run the project  
  `yarn build`
